#include <stdio.h>

int main(void) {
    
    //main loop
    while(){
        //listen for clients

        if() //when a client connects, add them to the active pool

        fork()//maybe active pool needs to be its own proccess?
    }

    return 0;
}

void activePool(){
    //when one user sends a chat request to another, pass it along
    //when the other user accepts, give them a port to chat along and connect them
}

void communityBoard(){
    //a community chat board people in the active pool can add to
}



