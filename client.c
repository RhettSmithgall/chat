#include <stdio.h>

int main(int argc,char* argv[]) {
    
    if(server){ //if no response from server, make one
        //make server
    }

    //if run with an argument, connect to argument user
    if(argc > 0){
        //send a connect request to ./chat <user>
    }

    //else

    //connect to server and to active pool

    //send to community board

    //send chat request to another user

    //accept chat request
}